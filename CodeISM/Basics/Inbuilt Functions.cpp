#include<bits/stdc++.h>
#define int long long
using namespace std;
int32_t main(){
    //Power function
    int x=pow(2,5);//returns 2^5, only for integers (int ,int)
    //if the numbers are small then only use pow function otherwise use binary exponentiation
    x=sqrt(5);//returns square root of the number
    cout<<x;
}